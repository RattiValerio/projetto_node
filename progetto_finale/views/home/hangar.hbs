<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hangar</title>
  <link rel="stylesheet" href="../css/aircrafts_style.css">
</head>
<body>
  <header class="header">
    <div class="container">
      <h1 class="logo">Hangar</h1>
      <nav class="nav">
        <a href="/hangar" class="nav-link active">Hangar</a>
        <a href="/aircrafts" class="nav-link">Aircrafts</a>
        <a href="/contact" class="nav-link">Contact Us</a>
      </nav>
    </div>
  </header>

  <main class="container">
  {{#if aircrafts.length}}
    <div class="grid">
      {{#each aircrafts}}
        <div class="card">
          <div class="card-body">
            <h3 class="card-title">{{aircraft}}</h3>
            <p class="card-detail"><strong>Purchase date:</strong> {{date}}</p>
            <p class="card-detail"><strong>Price:</strong> {{price}}</p>
            <form action="/hangar/remove/" method="POST" style="display:inline;">
              <input type="hidden" name="name" value="{{aircraft}}">
              <input type="hidden" name="date" value="{{date}}">
              <input type="hidden" name="price" value="{{price}}">
              <button type="submit" class="btn">Remove</button>
            </form>
          </div>
        </div>
      {{/each}}
    </div>
  {{else}}
    <p class="no-aircrafts">Empty there!</p>
  {{/if}}
</main>


  <footer class="footer">
    <div class="container">
      <p>&copy; 2024 Aircraft Shop. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
